torch>=2.1.0
numpy>=1.24
onnx>=1.16
onnxscript>=0.1.0

# 模型加载
transformers>=4.49.0,<4.50.0
accelerate>=0.30.0
safetensors>=0.4.0

# tokenizers 版本锁定 (transformers 依赖, 特定模型需要 0.22~0.23.0)
tokenizers>=0.22.0,<=0.23.0

# 各类 Tokenizer（不同模型可能需要不同的 tokenizer 库）
sentencepiece>=0.2.0
tiktoken>=0.7.0
protobuf>=3.20.0
